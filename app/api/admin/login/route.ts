import { NextRequest } from 'next/server'; export const runtime='nodejs'; export async function POST(req:NextRequest){ const {email,password}=await req.json(); if(email===process.env.ADMIN_EMAIL && password===process.env.ADMIN_PASSWORD){ return new Response(JSON.stringify({ok:true}),{headers:{'Set-Cookie':'admin=1; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=2592000'}}); } return new Response(JSON.stringify({error:'Invalid'}),{status:401}); }