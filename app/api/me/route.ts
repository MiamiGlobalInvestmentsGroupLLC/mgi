import { NextRequest } from 'next/server'; import { prisma } from '@/lib/prisma'; export const runtime='nodejs'; export async function GET(req:NextRequest){ const cid=req.cookies.get('companyId')?.value; if(!cid) return new Response(JSON.stringify({company:null}),{headers:{'Content-Type':'application/json'}}); const company=await prisma.company.findUnique({where:{id:cid}}); return new Response(JSON.stringify({company}),{headers:{'Content-Type':'application/json'}});}